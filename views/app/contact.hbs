<div class="ui body">
    <div class="ui container">
        <form action="" method="POST" role="form" class="ui form">
            <div class="four wide field">
                <label for="">名字</label>
                <input type="text" placeholder="请输入姓名" name="name">
            </div>
            <!--.form-group
            label.control-label(for='') 手机
            input.form-control(type='text', placeholder='请输入手机号', name='mobile')
            -->
            <div class="four wide field">
                <label for="">邮箱</label>
                <input type="text" placeholder="请输入邮箱" name="email">
            </div>
            <!--.form-group
            label(for='') 通讯地址
            input.form-control(type='text', placeholder='', name='address')
            -->
            <div class="field">
                <label for="">留言</label>
                <textarea placeholder="留言内容" name="content" rows="5"></textarea>
                <input type="hidden" name="_csrf" value="{{token}}">
            </div>
            <div class="inline field">
                <button type="submit" class="ui primary button">提交 </button>
            </div>
        </form>
    </div>
</div>